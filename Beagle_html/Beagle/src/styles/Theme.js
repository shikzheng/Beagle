import * as variables from './Variables';

export default {
  palette: {
    textColor: variables.primaryTextColor,
    primary1Color: variables.primaryColor,
    accent1Color: variables.primaryColor
  }
}